entrypoint = "main.py"
language = "python3"

run = "uvicorn main:app --host 0.0.0.0 --port 8000"

modules = ["nodejs-20", "python-3.11"]

[agent]
expertMode = true

[nix]
packages = ["espeak-ng", "file", "libxcrypt", "mailutils"]

[[ports]]
localPort = 8000
externalPort = 80
